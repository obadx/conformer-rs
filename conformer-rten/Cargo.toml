[package]
name = "conformer-rten"
version = "0.1.0"
edition = "2024"

[dependencies]
rten = { version = "0.24", features = ["onnx_format"] }
rten-tensor = "0.24"
clap = { version = "4", features = ["derive"] }
